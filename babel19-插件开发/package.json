{
    "scripts": {
        "build": "babel src -d dist",
        "debug": "node --inspect-brk node_modules/@babel/cli/bin/babel src -d dist"
    },
    "devDependencies": {
        "@babel/cli": "^7.0.0",
        "@babel/core": "^7.0.0"
    }
}